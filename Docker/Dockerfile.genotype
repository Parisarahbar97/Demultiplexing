# Minimal bcftools image for VCF prep
FROM staphb/bcftools:1.21

# (Optional) If your script uses bash-isms, ensure /bin/bash exists (it does on this base)
# Copy your prep script into the image
COPY scripts/prep_healthy_vcfs.sh /usr/local/bin/prep_healthy_vcfs.sh
RUN chmod +x /usr/local/bin/prep_healthy_vcfs.sh

WORKDIR /work
# Make the script the default entrypoint so we can just pass /in /out
ENTRYPOINT ["/usr/local/bin/prep_healthy_vcfs.sh"]
